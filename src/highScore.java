public class highScore {
    public highScore(int score) {
        var highScore = 0;
        if (score > highScore) {
            highScore = score;
            System.out.println("The high score is " + highScore);
        }
    }
}
